@model IEnumerable<GeradorDeProtocolo.Models.Assunto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" >Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AssuntoDescricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GrupoAssunto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AssuntoDescricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GrupoAssunto.Descricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AssuntoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AssuntoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AssuntoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
